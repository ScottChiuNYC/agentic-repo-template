# Reference Corpus

Place stable source PDFs below `ref/` when a project needs repository-backed reference ingestion.

Keep original source files authoritative. Generated transcripts are written below `ref/transcripts/`, versioned by the source Git blob SHA, and excluded from the default CodeBinder PDF to avoid recursive or oversized publication artifacts.
